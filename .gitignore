server.log
__pycache__/
*.pyc
.claude/settings.local.json
.DS_Store
.venv/

# Real client data exports (sensitive, under DPA) — never commit. Synthetic
# sample_data/*.csv / *.txt / *.json fixtures are fine and tracked.
sample_data/*.xlsx
sample_data/*.xls
# ...including CSV conversions of the real client exports (e.g. "Tickets Mercor v3.csv").
sample_data/*Mercor*.csv
# Larger sample is derived from the real export (real Dutch notes) — local only.
sample_data/sample_time_entries_large.csv

# Internal spec/status docs — kept local, not tracked.
specs/

# Local diagnostic scripts — kept local, not tracked.
list_batches.py
batch_durations.py
